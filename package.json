{
  "name": "",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc --build && echo 'index.js looks like this:\n' && cat ./lib/esm/index.js && echo"
  },
  "type": "module",
  "devDependencies": {
    "typescript": "latest"
  }
}
